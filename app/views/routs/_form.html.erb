<%= form_with(model: rout) do |form| %>
  <% if rout.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(rout.errors.count, "error") %> prohibited this train from being saved:</h2>

      <ul>
        <% rout.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
