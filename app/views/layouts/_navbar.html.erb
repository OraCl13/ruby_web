<% if user %>
<% if user.admin %>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="#">Rails</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link active" href="/admin/railway_stations">AdStations<span class="sr-only">(current)</span></a>
        <a class="nav-item nav-link" href="/admin/trains">AdTrains</a>
        <a class="nav-item nav-link" href="/admin/routs">AdRouts</a>
        <a class="nav-item nav-link" href="/admin/tickets">AdTickets</a>
        <a class="nav-item nav-link" href="/users">Users</a>
        <a class="nav-item nav-link" href="/search">New search</a>
      </div>
    </div>
  </nav>
<% else %>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="#">Rails</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link active" href="/search">Search<span class="sr-only">(current)</span></a>
        <a class="nav-item nav-link" href="/tickets">Bought Tickets</a>
      </div>
    </div>
  </nav>
<% end %>
  <% end %>

<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>

<% if user_signed_in? %>
  <%= button_to 'Exit', destroy_user_session_path, method: :delete %>
  <p>Account: <%= user.email %></p>
<% else %>
  <%= link_to 'Log in', new_user_session_path %>
<% end %>